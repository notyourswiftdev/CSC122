#include <iostream>

using namespace std;

int main() {


    // prompt the user to end a curreny amount
    
    // read the input in a c-style string

    // call the function you created above to convert
    // the amount. if the amount is valid, then display
    // the amount in the form of a real (double) amount.
    // prompt the user if they wish to enter a new value (yes/no)

    // *BONUS FOR ME* if they enter 100 cents round up to 1 dollar.
    // *DOUBLE BONUS FOR ME* any amount ending in .100 (42.100), round to 43 dollars
}